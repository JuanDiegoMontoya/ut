#version 450

layout(set = 0, binding = 0, std430) writeonly restrict buffer SSBO0
{
    uint data[];
}buf;

layout(local_size_x = 64) in;
void main()
{
    uint i = gl_GlobalInvocationID.x;
    buf.data[i] = i;
}